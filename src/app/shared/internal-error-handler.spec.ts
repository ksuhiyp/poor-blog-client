import { InternalErrorHandler } from './internal-error-handler';

describe('ErrorHandler', () => {
  it('should create an instance', () => {
    expect(new InternalErrorHandler()).toBeTruthy();
  });
});
